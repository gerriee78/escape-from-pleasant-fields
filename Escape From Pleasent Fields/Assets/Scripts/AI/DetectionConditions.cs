using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DetectionConditions : MonoBehaviour
{
    [SerializeField]
    enum DetectionCondition {Still,Moving,Unconditional,Call,Heal,Immunity}

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
